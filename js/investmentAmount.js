'use strict';

const e = React.createElement;

class investmentAmount extends React.Component {
  constructor(props) {
    super(props);
    this.state = { liked: false };
  }

  render() {
    if (this.state.liked) {
      return 'Hello world.';
    }

    return e(
      'button',
      { onClick: () => this.setState({ liked: true }) },
      'Like'
    );
  }
}

const domContainer = document.querySelector('#investmentAmount_component');
ReactDOM.render(e(investmentAmount), domContainer);